---
- name: Install and start Nginx
  hosts: all
  remote_user: ubuntu
  become: true
  become_user: root

  tasks:
    - name: Install Nginx
      apt:
        name: nginx
        state: present

    - name: Start and enable Nginx
      service:
        name: nginx
        state: started
        enabled: true
