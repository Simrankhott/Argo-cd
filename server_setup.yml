---
- name: Install and start Nginx
  hosts: all
  become: yes
  become_user: root
  remote_user: ubuntu

  tasks:
    - name: Install Nginx
      apt:
        name: nginx
        state: present

    - name: Start Nginx service
      service:
        name: nginx
        state: started
        enabled: yes
